export const metadata = {
  title: "Contact",
  description: "This space is not currently open to contact.",
};

export default function Contact() {
  return (
    <section className="w-full h-auto md:h-[75vh] border-b-2 border-solid border-dark dark:border-light flex items-center justify-center text-dark dark:text-light">
      {/* Intentionally empty */}
    </section>
  );
}